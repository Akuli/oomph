# TODO: util files shouldn't be needed in oomph projects

export func list_contains(List[Str] list, Str item) -> bool:
    foreach actual_item of list:
        if actual_item == item:
            return true
    return false

# TODO: replace this with a test involving a compilation error
import "../self_hosted/tokenizer.oomph" as tokenizer

export func main():
    print(tokenizer::tokenize("foo not in bar", "filename.oomph", 1, "").get(1))
